# 96. Auditoría de trazabilidad contra el Documento Maestro de Requerimientos v1.0

## Qué es esto

Re-lectura completa de los 3 documentos fuente oficiales (`InfoMatt_Core_Documentacion_Maestra_Definitiva.pdf`, `InfoMatt_Core_Arquitectura_Maestra.pdf`, `InfoMatt360_Documento_Maestro_Requerimientos_Especificaciones_v1.docx`) contra el estado real del código, para identificar qué queda pendiente de las Fases 0-4 ya cerradas y de los módulos agregados después (ERP, WAHA, DonorSync, ExtAPI, AI Audit, S3, Gobernanza, Asset Lock, Enlace Mágico, formularios públicos, importador multiformato, reemplazo de plantilla).

Los dos PDFs son narrativa de arquitectura de alto nivel (infraestructura Docker, los "7 súper-módulos" ya asimilados, roadmap por fases) y no aportan requisitos nuevos no cubiertos antes. El `.docx` (27 secciones) es mucho más granular y es la fuente de los 14 hallazgos de abajo.

## Hallazgos (verificados leyendo el código, no asumidos)

| # | Requisito (doc §) | Veredicto | Evidencia |
|---|---|---|---|
| 1 | **Base Espejo** — replicación hacia BD externa (Postgres/MySQL/SQL Server/SQLite/ODBC), con prueba de conexión, creación de estructura equivalente y sincronización programada/por eventos en modos insert-only/incremental/espejo completo/respaldo analítico (§17) | ~~PARCIAL~~ **PARCIAL (motor real)** | Motor real para Postgres y SQLite: `POST /mirror/targets/{id}/test-connection` abre una conexión real, `POST /mirror/plans/{id}/run` crea la estructura equivalente (`im360_runtime_records`/`im360_runtime_record_values`) y sincroniza en modo `full_mirror` o `insert_only`, credenciales cifradas — [docs/102](102_BASE_ESPEJO_REAL.md). Pendiente, documentado honestamente: MySQL/SQL Server, modo incremental, tabla ancha pivotada, integración con `ScheduledTask` (hoy solo disparo manual), sin UI de frontend. |
| 2 | Carga masiva por Excel de **asignaciones** (usuarios↔proyectos↔formularios↔territorios↔dispositivos↔permisos) (§10) | ~~FALTA~~ **PARCIAL** | `entity_type="assignments"` en el mismo pipeline subir→previsualizar→mapear→aprobar, crea `UserProjectAssignment` (usuario+proyecto+rol) reutilizando usuarios/roles ya existentes — [docs/103](103_CARGA_MASIVA_DE_ASIGNACIONES.md). Acotado con el usuario: no incluye formulario/territorio/dispositivo porque esas entidades (`Territory`, `Device`) no existen en el modelo de datos; construirlas era un feature aparte. |
| 3 | Carga masiva de **registros históricos/externos** de respuestas por Excel, pasando por el mismo peaje de validación (§10) | ~~PARCIAL~~ **RESUELTO** | `entity_type="records"` en el mismo pipeline, mapeo dinámico contra los campos reales de la plantilla, reutiliza `runtime_record_service.save_record()` fila por fila (mismo peaje: enlace de participante, detección de duplicado, flujo de aprobación, consecutivos) — [docs/104](104_CARGA_MASIVA_DE_REGISTROS_HISTORICOS.md). Solo tipos de campo escalares simples; archivos/GPS/subformularios/multiselect/calculados quedan documentados como pendientes. |
| 4 | **Constructor visual** de actas/documentos (arrastrar logos, encabezados, tablas, firmas) (§7, §16) | ~~FALTA~~ **RESUELTO** | Constructor visual con 4 bloques (logo/encabezado/tabla/firma) resueltos contra un registro Runtime real, branding de organización inyectado automáticamente — [docs/109](109_CONSTRUCTOR_VISUAL_ACTAS.md). El camino legado (`html_template` Jinja2 crudo, sin UI) se conserva como vía de escape avanzada por API. |
| 5 | Generación **masiva** de actas PDF (no solo una por una) (§15, §16) | ~~FALTA~~ **RESUELTO** | `POST /acta-templates/{id}/render-batch` — selección por filtro o manual, ZIP con `manifest.csv`, tope de 200 registros/lote, fallos parciales no abortan el lote — [docs/110](110_GENERACION_MASIVA_ACTAS.md). |
| 6 | Constructor visual de **reportes/tableros simples** (§7) | ~~FALTA~~ **RESUELTO** | `ReportBoard` (una configuración por proyecto): widgets KPI/tabla/gráfico (barra y torta, SVG propio sin dependencia nueva) más métricas personalizadas (campo + agregación) — [docs/111](111_CONSTRUCTOR_VISUAL_REPORTES.md). |
| 7 | Descarga masiva de evidencias en **ZIP** filtrada por participante/formulario/proyecto/fecha/estado/gestor, con renombrado automático (§18) | ~~FALTA~~ **RESUELTO** | `POST /files/project/{id}/download-batch` — filtro combinado (gestor mapeado a `FileAsset.created_by`), renombrado `Participante_TipoEvidencia_Fecha`, tope doble de cantidad/peso, lectura desde disco local y S3 — [docs/112](112_DESCARGA_MASIVA_EVIDENCIAS.md). |
| 8 | **GIS real**: columnas de geometría PostGIS, GeoJSON, WMS/WFS (§19) | ~~PARCIAL~~ **RESUELTO** | Columna `geom` dialect-aware (PostGIS/GeoAlchemy2 real en Postgres, texto de respaldo en SQLite), búsqueda por radio real (`ST_DWithin` en Postgres, Haversine en Python en SQLite), mapa Leaflet+OSM con marcadores/líneas/polígonos reales — [docs/113](113_GIS_REAL_POSTGIS_CONDICIONAL.md). Decisión de alcance acordada: WMS/WFS literal se sustituye por mapa interactivo + API GeoJSON con búsqueda por radio, mismo propósito práctico. |
| 9 | Conectores formales para Power BI / Looker / Tableau / Metabase / Superset (§19) | ~~FALTA~~ **PARCIAL** | Conectores REST reales para Power BI (snippet Power Query M documentado) y Tableau (Web Data Connector construido y probado en vivo con la librería oficial de Tableau) — [docs/114](114_CONECTORES_BI_POWER_BI_TABLEAU.md). Looker/Metabase/Superset necesitan conexión SQL real, no REST — remitidos explícitamente a madurar la función Mirror/Base Espejo (hallazgo #1), no construidos como conectores simulados. |
| 10 | Selección de impresora e impresión masiva/individual desde el **cliente de escritorio** (§15) | ~~FALTA~~ **RESUELTO** | `desktop/src/printing.js` (Electron `webContents.print`, selector de impresora nativo), impresión individual y en lote de actas ya generadas — [docs/115](115_IMPRESION_NATIVA_ESCRITORIO_ACTAS.md). Límite honesto: la llamada nativa final (impresora física real) no se pudo ejercitar en este entorno de desarrollo, solo por revisión de código y verificación con bridge falso. |
| 11 | **Doble bandeja de correo** (bandeja interna + bandeja externa vía IMAP) (§12) | ~~FALTA~~ **RESUELTO** | Bandeja externa de solo lectura, un perfil IMAP por proyecto, sondeo horario vía `ScheduledTask` (`task_type="mail_poll"`) — [docs/116](116_BANDEJA_EXTERNA_IMAP.md). De paso, corrige una fuga real: `MailProfile.config_json` (incluida la contraseña SMTP) pasó de texto plano a cifrado, y dejó de exponerse en las respuestas de la API. |
| 12 | Jerarquía de roles: **Administrador nacional** (sobre todas las organizaciones) vs. **Administrador de proyecto** vs. **Auditor/Consulta** de solo lectura (§21) | ~~FALTA~~ **RESUELTO** | `UserOrganizationAssignment` (rol a nivel de Organización, cubre todos sus proyectos presentes y futuros = "Administrador nacional", sin cruzar a otras organizaciones), rol predefinido "Auditor/Consulta" sembrado por el instalador en toda organización nueva — [docs/101](101_JERARQUIA_DE_ROLES_ORGANIZACION.md). De paso corrige un hueco real: `POST/GET /assignments/` no tenía ningún chequeo de permiso. |
| 13 | Vista unificada del **historial del participante** a través de todos los canales (§9) | ~~FALTA~~ **RESUELTO** | `RuntimeRecord` enlaza a `Participant` (explícito o por coincidencia de `DOCUMENT_ID`), historial unificado entre plantillas/canales expuesto en `/participants` — [docs/98](98_PARTICIPANTE_COMO_EJE_CENTRAL.md). |
| 14 | Máquina de estados completa: borrador, enviado, en revisión, devuelto, corregido, aprobado, rechazado, **anulado**, **sincronizado** (§13) | ~~PARCIAL~~ **RESUELTO** | `voided` (anulado, terminal, requiere `records.void`) y `synced` (sincronizado, solo desde `approved`, automático cuando la integración de donante confirma envío o manual) — [docs/100](100_ESTADOS_ANULADO_Y_SINCRONIZADO.md). Cierra los 9 estados mínimos del documento maestro. |

## Lo que NO es una brecha (ya resuelto, confirmado en auditorías previas)

Backups automáticos, edición concurrente (enlace mágico + asset lock), captura pública por token, instalador multi-paso completo, ERP headless, WhatsApp/WAHA, auditoría semántica IA, storage S3/MinIO/Google Drive, mesa de ayuda, multi-tenancy lógico (Organization), branding dinámico, PWA offline, escritorio Electron básico, XLSForm import/export + plantilla maestra, importador multiformato (SurveyMonkey/LimeSurvey), reemplazo de plantilla en el mismo lugar — ver [[reference-published-artifacts]] y docs/59-95.

## Nota: auditoría técnica externa (documento distinto, julio 2026)

Esta auditoría (docs/96) es una lectura de trazabilidad contra el documento maestro de requerimientos — no debe confundirse con la **auditoría técnica externa** que el usuario compartió por separado en julio de 2026 (`AUDITORIA_TECNICA_INTEGRAL_INFOMATT360.md`, no versionada en el repo), centrada en arquitectura/seguridad/escalabilidad/observabilidad más que en cobertura de funcionalidad. Esa auditoría se trabajó aparte, triada en 4 categorías (A: fixes de seguridad baratos, B: features de sincronización, C: decisiones de infraestructura, D: refactors grandes y riesgosos).

**Categoría C de esa auditoría técnica externa quedó completamente cerrada el 2026-07-23**: caché de permisos (E-004, docs/108), balanceo de carga + réplicas del backend (E-001, docs/117), stack de observabilidad Prometheus/Grafana (docs/118), script de prueba de carga k6 (docs/119) y PgBouncer (E-003, docs/120) — todos verificados en vivo con Podman, no solo validados estáticamente. Las categorías A y B de esa misma auditoría también están cerradas. Solo queda abierta la categoría D (descomponer la entidad `User`, migrar SQLAlchemy síncrono a async), explícitamente diferida hasta contar con evidencia real de carga a escala de producción.

## Decisión arquitectónica ya tomada, no una brecha

El aislamiento fuerte "base independiente por proyecto o schema-per-project" (§2) fue evaluado explícitamente al inicio de este ciclo de trabajo y el usuario decidió el modelo de **tenant lógico** (`Organization` sobre `Project`, aislamiento a nivel de aplicación) en vez de aislamiento físico — ver el plan `declarative-spinning-simon.md`. No se re-abre esta decisión salvo que el usuario lo pida explícitamente.

## Estado final: los 14 hallazgos originales están cerrados

Cerrados en orden: #13 ([docs/98](98_PARTICIPANTE_COMO_EJE_CENTRAL.md)), #2/#3 ([docs/103](103_CARGA_MASIVA_DE_ASIGNACIONES.md)/[docs/104](104_CARGA_MASIVA_DE_REGISTROS_HISTORICOS.md)), #4-#11 ([docs/109](109_CONSTRUCTOR_VISUAL_ACTAS.md)-[docs/116](116_BANDEJA_EXTERNA_IMAP.md)), y finalmente #1/#12/#14 ([docs/102](102_BASE_ESPEJO_REAL.md)/[docs/101](101_JERARQUIA_DE_ROLES_ORGANIZACION.md)/[docs/100](100_ESTADOS_ANULADO_Y_SINCRONIZADO.md)) el 2026-07-23.

**Ninguno quedó al 100% del texto original del documento maestro** — cada cierre tiene al menos un recorte de alcance acordado explícitamente con el usuario, documentado en su propio `docs/NNN`:

- **#1 (Base Espejo)**: motor real solo para Postgres/SQLite, disparo manual (sin integración con `ScheduledTask`), sin MySQL/SQL Server, sin modo incremental, sin UI de frontend.
- **#2 (asignaciones)**: acotado a usuario+proyecto+rol; formulario/territorio/dispositivo no tienen modelo de datos propio en este repo.
- **#3 (registros históricos)**: solo tipos de campo escalares simples, sin archivos/GPS/subformularios/multiselect/calculados.
- **#8 (GIS)**: WMS/WFS literal sustituido por mapa Leaflet + API GeoJSON de radio.
- **#9 (conectores BI)**: solo Power BI/Tableau tienen conector propio; Looker/Metabase/Superset quedan remitidos a conectarse directo a la base espejo de #1.
- **#10 (impresión desktop)**: la llamada nativa final a una impresora física real no se pudo ejercitar en este entorno de desarrollo, solo revisión de código.

Ninguno de estos recortes es un olvido — todos fueron decisiones explícitas del usuario (vía `AskUserQuestion` en el momento de cada implementación) para no construir capacidades que exigían entidades de datos inexistentes o infraestructura no disponible en este entorno. Si se quiere cerrar alguno de estos residuos, es trabajo nuevo, no una corrección de algo mal hecho.
